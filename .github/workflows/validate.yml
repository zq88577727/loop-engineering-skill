name: Validate

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run repository validator
        run: python3 scripts/validate_repo.py

      - name: Compile Python scripts
        run: python3 -m py_compile scripts/validate_repo.py skills/loop-engineering/scripts/init_loop_project.py

      - name: Run unit tests
        run: python3 -m unittest discover -s tests

      - name: Run initializer smoke test
        run: python3 skills/loop-engineering/scripts/init_loop_project.py --target /tmp/loop-engineering-smoke
